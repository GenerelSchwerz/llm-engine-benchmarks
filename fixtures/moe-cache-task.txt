You are reviewing a GPU cache of CPU-resident Mixture-of-Experts weights. A router selects experts for each token. A background worker may copy likely next experts to GPU memory, while the demand path must always use the experts actually selected by the router.

Explain a safe design for this cache in plain technical language. Cover how cache entries are identified, when a prefetched entry is safe to consume, what happens if a prefetch is late or predicts the wrong expert, how eviction avoids racing with an in-flight GPU read, and what measurements would show whether the cache helps. Include one small example with two consecutive tokens and a wrong prediction. Finish with two failure cases you would test.
