# Private tmux session for an embedded Codex agent.
set -g mouse on
set -g history-limit 100000
set -g remain-on-exit on
set -g status off
set -g default-terminal 'tmux-256color'
bind-key -T root WheelUpPane copy-mode -e \; send-keys -X -N 5 scroll-up
