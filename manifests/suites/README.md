# Frozen suite revisions

Create a new dated directory for each live sweep. Save the read-only source check JSON, accepted command packets, exact model/tool/fixture hashes, hardware cohort, and the final run plan before starting any server. Never edit a frozen plan after a run begins; create another revision for changes.
